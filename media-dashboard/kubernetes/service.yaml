apiVersion: v1
kind: Service
metadata:
  name: media-dashboard
  namespace: analytics-tools
  labels:
    app: media-dashboard
spec:
  type: ClusterIP
  selector:
    app: media-dashboard
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
