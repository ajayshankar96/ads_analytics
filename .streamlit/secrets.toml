# Trino/Presto Database Configuration
  trino_host = "trino-gateway-router-looker.de.razorpay.com"
  trino_port = 443
  trino_catalog = "hive"
  trino_schema = "default"

  # Note: Users will enter their own credentials in the app UI
  # No passwords stored in this file for security
