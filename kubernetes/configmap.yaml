apiVersion: v1
kind: ConfigMap
metadata:
  name: trustscan-config
  namespace: analytics-tools
data:
  TRINO_HOST: "trino.razorpay.com"  # Update with actual Trino host
  TRINO_PORT: "443"
  TRINO_HTTP_SCHEME: "https"
  TRINO_CATALOG: "hive"
  TRINO_SCHEMA: "aggregate_ba"
